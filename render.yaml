services:
  - type: web
    name: chart-bot-ai
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: GROQ_API_KEY
        value: your_groq_api_key_here
